apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      # port: 8080
      port: 80
      targetPort: 80
  clusterIP: 10.43.0.69
  type: LoadBalancer
